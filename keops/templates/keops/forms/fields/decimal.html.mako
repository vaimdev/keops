<input type="text" ui-money ng-model="form.item.${field.name}" name="${field.name}" id="${field.auto_id}" ng-show="form.write && !form.readonly.${field.name}" ${' '.join('%s="%s"' % (k, v) for k, v in field.field.widget.attrs.items())}>